# Phase 2.1 — 공유 인프라 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 두 MCP 서버가 공유할 핵심 인프라(설정/로깅/HTTP/검색/광고필터/응답빌더) + FastMCP 서버 골격을 구현하고 단위 테스트 통과시키기.

**Architecture:** Python 3.11 + FastMCP + httpx (async) + pydantic v2. `shared/` 모듈은 두 서버가 import 해서 사용. 모든 외부 호출은 비동기, 네이버 캐싱 X, Tavily 캐싱 O.

**Tech Stack:** mcp[cli]>=1.2.0, httpx, pydantic>=2, pytest, pytest-asyncio, pytest-httpx, python-dotenv.

**기간**: 2026-06-23 ~ 2026-06-26 (4일)

**선행 조건**:
- Python 3.11+ 설치되어 있어야 함
- Git 사용 가능
- 네이버 검색 API Client ID/Secret 발급 받아둠 (사용자 외주 #1 결과)
- Tavily API Key 발급 받아둠

---

## Task 1: 프로젝트 환경 + 의존성 셋업

**Files:**
- Create: `pyproject.toml`
- Create: `.env.example`
- Modify: `.gitignore` (이미 .env 등록되어 있음 — 확인만)

- [ ] **Step 1: Python 3.11+ 확인**

```bash
python --version
```

Expected: `Python 3.11.x` 이상. 3.10 이하면 업그레이드 필요.

- [ ] **Step 2: 가상환경 생성 및 활성화**

```bash
cd /c/Users/11/Documents/agentic-player-10
python -m venv .venv
```

Windows Git Bash 활성화:
```bash
source .venv/Scripts/activate
```

Windows PowerShell 활성화:
```powershell
.venv\Scripts\Activate.ps1
```

확인:
```bash
which python
```
Expected: `.venv` 경로 포함

- [ ] **Step 3: pyproject.toml 작성**

`pyproject.toml`:
```toml
[project]
name = "agentic-player-10"
version = "0.1.0"
description = "AGENTIC PLAYER 10 — Hyodo Secretary + Gift Curator MCP servers"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-httpx>=0.30.0",
    "ruff>=0.5.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*", "servers*"]
```

- [ ] **Step 4: 의존성 설치**

```bash
pip install --upgrade pip
pip install -e ".[dev]"
```

Expected: 모든 패키지 설치 완료. 마지막 줄에 `Successfully installed ...`.

- [ ] **Step 5: MCP SDK 설치 검증**

```bash
python -c "from mcp.server.fastmcp import FastMCP; print('FastMCP OK')"
```

Expected: `FastMCP OK` 출력. 에러 시 `pip install mcp[cli]` 재시도.

- [ ] **Step 6: .env.example 작성**

`.env.example`:
```bash
# 네이버 검색 API (developers.naver.com에서 발급)
NAVER_CLIENT_ID=your_naver_client_id_here
NAVER_CLIENT_SECRET=your_naver_client_secret_here

# Tavily API (tavily.com에서 발급)
TAVILY_API_KEY=tvly-dev-your_key_here

# 로깅 레벨
LOG_LEVEL=INFO
```

- [ ] **Step 7: 본인 .env 파일 작성 (커밋 X)**

`.env` (이미 .gitignore에 등록되어 있음):
```bash
NAVER_CLIENT_ID=<실제 발급 받은 값>
NAVER_CLIENT_SECRET=<실제 발급 받은 값>
TAVILY_API_KEY=tvly-dev-<실제 발급 받은 값>
LOG_LEVEL=INFO
```

⚠️ `.env`는 절대 git에 커밋하지 마세요. `.gitignore`에 이미 등록되어 있는지 확인:
```bash
grep "^\.env" .gitignore
```
Expected: `.env`, `.env.*` 라인 보임

- [ ] **Step 8: 커밋**

```bash
git add pyproject.toml .env.example
git commit -m "chore: Python 환경 + 의존성 셋업 (Phase 2.1 Task 1)"
```

---

## Task 2: `shared/config.py` — 설정 로딩

**Files:**
- Create: `shared/__init__.py` (비어있음)
- Create: `shared/config.py`
- Create: `tests/__init__.py` (비어있음)
- Create: `tests/test_config.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_config.py`:
```python
import os
import pytest
from shared.config import Settings, get_settings


def test_settings_from_env(monkeypatch):
    monkeypatch.setenv("NAVER_CLIENT_ID", "test_id")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "test_secret")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-test")

    settings = Settings.from_env()

    assert settings.naver_client_id == "test_id"
    assert settings.naver_client_secret == "test_secret"
    assert settings.tavily_api_key == "tvly-test"
    assert settings.log_level == "INFO"
    assert settings.response_max_chars == 22000


def test_settings_missing_required_raises(monkeypatch):
    monkeypatch.delenv("NAVER_CLIENT_ID", raising=False)

    with pytest.raises(ValueError, match="NAVER_CLIENT_ID"):
        Settings.from_env()


def test_get_settings_caches(monkeypatch):
    monkeypatch.setenv("NAVER_CLIENT_ID", "id1")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "s1")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-1")

    s1 = get_settings()
    s2 = get_settings()
    assert s1 is s2  # 캐싱 확인
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_config.py -v
```

Expected: `ModuleNotFoundError: No module named 'shared.config'` 또는 import 에러로 FAIL.

- [ ] **Step 3: shared/__init__.py 생성 (빈 파일)**

```bash
touch shared/__init__.py
touch tests/__init__.py
```

(Windows에서는 `New-Item shared/__init__.py -ItemType File` 또는 텍스트 에디터로 빈 파일 생성)

- [ ] **Step 4: shared/config.py 구현**

`shared/config.py`:
```python
"""환경 변수 로딩 + 설정 객체."""
from __future__ import annotations

import os
from dataclasses import dataclass
from functools import lru_cache

from dotenv import load_dotenv

load_dotenv()  # .env 파일 자동 로딩


@dataclass(frozen=True)
class Settings:
    naver_client_id: str
    naver_client_secret: str
    tavily_api_key: str
    log_level: str = "INFO"
    naver_max_per_query: int = 10
    tavily_max_results: int = 5
    response_max_chars: int = 22000  # 24k 한계 - 2k 버퍼
    external_call_timeout: float = 5.0
    external_call_concurrency: int = 5

    @classmethod
    def from_env(cls) -> "Settings":
        required = ["NAVER_CLIENT_ID", "NAVER_CLIENT_SECRET", "TAVILY_API_KEY"]
        for key in required:
            if not os.environ.get(key):
                raise ValueError(f"필수 환경 변수 누락: {key}. .env 파일 또는 환경 변수에 설정해주세요.")

        return cls(
            naver_client_id=os.environ["NAVER_CLIENT_ID"],
            naver_client_secret=os.environ["NAVER_CLIENT_SECRET"],
            tavily_api_key=os.environ["TAVILY_API_KEY"],
            log_level=os.environ.get("LOG_LEVEL", "INFO"),
        )


@lru_cache(maxsize=1)
def get_settings() -> Settings:
    """모듈 전역 캐시된 Settings."""
    return Settings.from_env()
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
pytest tests/test_config.py -v
```

Expected: 3 passed.

만약 `lru_cache`가 monkeypatch와 충돌하면, `get_settings.cache_clear()` 호출 fixture 추가:
```python
@pytest.fixture(autouse=True)
def clear_settings_cache():
    get_settings.cache_clear()
    yield
    get_settings.cache_clear()
```

- [ ] **Step 6: 커밋**

```bash
git add shared/__init__.py shared/config.py tests/__init__.py tests/test_config.py
git commit -m "feat(shared): config.py — 환경 변수 로딩 + Settings (Task 2)"
```

---

## Task 3: `shared/logging.py` — 표준 로거

**Files:**
- Create: `shared/logging.py`
- Create: `tests/test_logging.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_logging.py`:
```python
import logging
from shared.logging import setup_logger


def test_setup_logger_returns_logger():
    logger = setup_logger("test_service")
    assert isinstance(logger, logging.Logger)
    assert logger.name == "test_service"


def test_logger_uses_configured_level(monkeypatch):
    monkeypatch.setenv("LOG_LEVEL", "DEBUG")
    # config 재로딩 위해 cache clear
    from shared.config import get_settings
    get_settings.cache_clear()

    logger = setup_logger("test_service_debug")
    assert logger.level == logging.DEBUG


def test_logger_outputs_json_format(caplog):
    logger = setup_logger("test_service_json")
    with caplog.at_level(logging.INFO):
        logger.info("test_message", extra={"tool": "compose_anbu", "duration_ms": 42})

    assert any("test_message" in record.message for record in caplog.records)
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_logging.py -v
```

Expected: `ModuleNotFoundError: No module named 'shared.logging'`.

- [ ] **Step 3: shared/logging.py 구현**

`shared/logging.py`:
```python
"""표준 로깅 설정 — JSON 친화적 포맷, 카카오클라우드 로그 수집 호환."""
from __future__ import annotations

import json
import logging
import sys
from typing import Any

from shared.config import get_settings


class JsonFormatter(logging.Formatter):
    """간단한 JSON 포맷터. 카카오클라우드 로그 수집 친화."""

    def format(self, record: logging.LogRecord) -> str:
        payload: dict[str, Any] = {
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
            "time": self.formatTime(record, datefmt="%Y-%m-%dT%H:%M:%S"),
        }
        # extra fields
        for key, value in record.__dict__.items():
            if key not in ("args", "asctime", "created", "exc_info", "exc_text",
                            "filename", "funcName", "levelname", "levelno", "lineno",
                            "module", "msecs", "message", "msg", "name", "pathname",
                            "process", "processName", "relativeCreated", "stack_info",
                            "thread", "threadName"):
                payload[key] = value
        return json.dumps(payload, ensure_ascii=False)


def setup_logger(name: str) -> logging.Logger:
    """이름별 로거 생성. 같은 이름 호출 시 같은 객체 반환."""
    logger = logging.getLogger(name)
    if logger.handlers:
        # 이미 설정됨 (idempotent)
        return logger

    settings = get_settings()
    logger.setLevel(getattr(logging, settings.log_level.upper(), logging.INFO))

    handler = logging.StreamHandler(sys.stdout)
    handler.setFormatter(JsonFormatter())
    logger.addHandler(handler)
    logger.propagate = False

    return logger
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_logging.py -v
```

Expected: 3 passed.

- [ ] **Step 5: 커밋**

```bash
git add shared/logging.py tests/test_logging.py
git commit -m "feat(shared): logging.py — JSON 포맷 로거 (Task 3)"
```

---

## Task 4: `shared/http_client.py` — 비동기 HTTP 클라이언트

**Files:**
- Create: `shared/http_client.py`
- Create: `tests/test_http_client.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_http_client.py`:
```python
import pytest
import httpx
from pytest_httpx import HTTPXMock

from shared.http_client import HttpClient


@pytest.mark.asyncio
async def test_get_success(httpx_mock: HTTPXMock):
    httpx_mock.add_response(json={"hello": "world"})
    client = HttpClient()
    result = await client.get("https://example.com/api")
    assert result == {"hello": "world"}
    await client.aclose()


@pytest.mark.asyncio
async def test_get_with_timeout(httpx_mock: HTTPXMock):
    httpx_mock.add_exception(httpx.TimeoutException("Timeout"))
    client = HttpClient()
    with pytest.raises(httpx.TimeoutException):
        await client.get("https://example.com/slow", timeout=0.5)
    await client.aclose()


@pytest.mark.asyncio
async def test_get_cache_hit_when_ttl_set(httpx_mock: HTTPXMock):
    """cache_ttl > 0 일 때 두 번째 호출은 캐시 적중."""
    httpx_mock.add_response(json={"value": 1})
    client = HttpClient()

    r1 = await client.get("https://example.com/cacheable", cache_ttl=60)
    r2 = await client.get("https://example.com/cacheable", cache_ttl=60)

    assert r1 == r2 == {"value": 1}
    # 두 번째 호출이 캐시에서 왔으므로 HTTP 요청은 1회만
    assert len(httpx_mock.get_requests()) == 1
    await client.aclose()


@pytest.mark.asyncio
async def test_get_no_cache_by_default(httpx_mock: HTTPXMock):
    """cache_ttl=0 (기본) 일 때 매번 호출."""
    httpx_mock.add_response(json={"value": 1})
    httpx_mock.add_response(json={"value": 2})
    client = HttpClient()

    r1 = await client.get("https://example.com/no-cache")
    r2 = await client.get("https://example.com/no-cache")

    assert r1 == {"value": 1}
    assert r2 == {"value": 2}
    assert len(httpx_mock.get_requests()) == 2
    await client.aclose()
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_http_client.py -v
```

Expected: `ModuleNotFoundError`.

- [ ] **Step 3: shared/http_client.py 구현**

`shared/http_client.py`:
```python
"""비동기 HTTP 클라이언트 — 서비스별 캐싱 정책 + 타임아웃 + 재시도."""
from __future__ import annotations

import asyncio
import time
from typing import Any

import httpx

from shared.config import get_settings
from shared.logging import setup_logger

logger = setup_logger("http_client")


class HttpClient:
    """httpx.AsyncClient 래퍼.

    - 기본 타임아웃 5초
    - cache_ttl=0 (기본) → 캐싱 안 함 (네이버 약관 준수)
    - cache_ttl>0 → 해당 초만큼 단순 in-memory 캐싱 (Tavily용)
    - 동시 호출 제한 (Semaphore)
    """

    def __init__(self) -> None:
        settings = get_settings()
        self._client = httpx.AsyncClient(timeout=settings.external_call_timeout)
        self._semaphore = asyncio.Semaphore(settings.external_call_concurrency)
        self._cache: dict[str, tuple[float, Any]] = {}  # key -> (expire_at, value)

    def _cache_key(self, url: str, params: dict | None) -> str:
        if not params:
            return url
        items = sorted(params.items())
        return url + "?" + "&".join(f"{k}={v}" for k, v in items)

    def _cache_get(self, key: str) -> Any | None:
        entry = self._cache.get(key)
        if entry is None:
            return None
        expire_at, value = entry
        if time.time() > expire_at:
            del self._cache[key]
            return None
        return value

    def _cache_set(self, key: str, value: Any, ttl: float) -> None:
        self._cache[key] = (time.time() + ttl, value)

    async def get(
        self,
        url: str,
        *,
        headers: dict | None = None,
        params: dict | None = None,
        timeout: float | None = None,
        cache_ttl: float = 0,
    ) -> dict:
        """GET 요청. cache_ttl=0 = 캐싱 안 함."""
        if cache_ttl > 0:
            key = self._cache_key(url, params)
            cached = self._cache_get(key)
            if cached is not None:
                logger.info("http_cache_hit", extra={"url": url})
                return cached

        async with self._semaphore:
            response = await self._client.get(
                url, headers=headers, params=params, timeout=timeout
            )
            response.raise_for_status()
            data = response.json()

        if cache_ttl > 0:
            self._cache_set(self._cache_key(url, params), data, cache_ttl)

        return data

    async def aclose(self) -> None:
        await self._client.aclose()
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_http_client.py -v
```

Expected: 4 passed.

- [ ] **Step 5: 커밋**

```bash
git add shared/http_client.py tests/test_http_client.py
git commit -m "feat(shared): http_client.py — async httpx 래퍼 + 캐싱 정책 분리 (Task 4)"
```

---

## Task 5: `shared/naver_search.py` — 네이버 블로그/카페 검색

**Files:**
- Create: `shared/naver_search.py`
- Create: `tests/test_naver_search.py`

⚠️ 네이버 약관 — 캐싱 X. 출처 표기 의무.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_naver_search.py`:
```python
import pytest
from pytest_httpx import HTTPXMock

from shared.http_client import HttpClient
from shared.naver_search import NaverSearch


@pytest.mark.asyncio
async def test_search_blog(httpx_mock: HTTPXMock, monkeypatch):
    monkeypatch.setenv("NAVER_CLIENT_ID", "test_id")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "test_secret")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-test")
    from shared.config import get_settings
    get_settings.cache_clear()

    httpx_mock.add_response(
        url="https://openapi.naver.com/v1/search/blog.json?query=수면+안마기&display=5",
        json={
            "total": 100,
            "items": [
                {"title": "<b>수면 안마기</b> 후기", "link": "https://blog.naver.com/x/1", "description": "써본 결과 좋아요", "bloggername": "ㅇㅇ"},
                {"title": "엄마 환갑 선물", "link": "https://blog.naver.com/y/2", "description": "마사지건 추천", "bloggername": "ㅁㅁ"},
            ],
        },
    )

    client = HttpClient()
    naver = NaverSearch(http=client)
    results = await naver.search("수면 안마기", source="blog", max_results=5)

    assert len(results) == 2
    assert results[0]["link"] == "https://blog.naver.com/x/1"
    # HTML 태그 제거 검증
    assert "<b>" not in results[0]["title"]
    await client.aclose()


@pytest.mark.asyncio
async def test_search_does_not_cache(httpx_mock: HTTPXMock, monkeypatch):
    """약관 준수 — 같은 query 두 번 호출하면 두 번 다 HTTP 요청."""
    monkeypatch.setenv("NAVER_CLIENT_ID", "test_id")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "test_secret")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-test")
    from shared.config import get_settings
    get_settings.cache_clear()

    httpx_mock.add_response(json={"items": []})
    httpx_mock.add_response(json={"items": []})

    client = HttpClient()
    naver = NaverSearch(http=client)
    await naver.search("test", source="blog")
    await naver.search("test", source="blog")

    assert len(httpx_mock.get_requests()) == 2  # 캐싱 안 됨
    await client.aclose()
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_naver_search.py -v
```

- [ ] **Step 3: 구현**

`shared/naver_search.py`:
```python
"""네이버 검색 API 클라이언트 (블로그/카페).

⚠️ 약관: 검색 결과 캐싱 금지 (API 결과 무단 복제·저장 금지 조항).
출처 표기 의무 — 응답 호출처에서 '네이버 검색 기반' 표기 필수.
"""
from __future__ import annotations

import re
from typing import Any, Literal

from shared.config import get_settings
from shared.http_client import HttpClient
from shared.logging import setup_logger

logger = setup_logger("naver_search")

_TAG_RE = re.compile(r"<[^>]+>")


def _strip_html_tags(text: str) -> str:
    return _TAG_RE.sub("", text)


class NaverSearch:
    BLOG_URL = "https://openapi.naver.com/v1/search/blog.json"
    CAFE_URL = "https://openapi.naver.com/v1/search/cafearticle.json"

    def __init__(self, http: HttpClient) -> None:
        self._http = http
        self._settings = get_settings()

    async def search(
        self,
        query: str,
        *,
        source: Literal["blog", "cafe"] = "blog",
        max_results: int = 10,
    ) -> list[dict[str, Any]]:
        url = self.BLOG_URL if source == "blog" else self.CAFE_URL
        headers = {
            "X-Naver-Client-Id": self._settings.naver_client_id,
            "X-Naver-Client-Secret": self._settings.naver_client_secret,
        }
        params = {"query": query, "display": min(max_results, 10)}

        # 약관 준수 — cache_ttl=0 (기본)
        data = await self._http.get(url, headers=headers, params=params)

        items = data.get("items", [])
        # HTML 태그 제거 (네이버는 <b> 태그로 검색어 강조)
        for item in items:
            item["title"] = _strip_html_tags(item.get("title", ""))
            item["description"] = _strip_html_tags(item.get("description", ""))

        logger.info("naver_search", extra={"query": query, "source": source, "count": len(items)})
        return items
```

- [ ] **Step 4: 테스트 통과**

```bash
pytest tests/test_naver_search.py -v
```

Expected: 2 passed.

- [ ] **Step 5: 커밋**

```bash
git add shared/naver_search.py tests/test_naver_search.py
git commit -m "feat(shared): naver_search.py — 네이버 블로그/카페 검색 (캐싱 X) (Task 5)"
```

---

## Task 6: `shared/tavily_search.py` — Tavily 글로벌 검색

**Files:**
- Create: `shared/tavily_search.py`
- Create: `tests/test_tavily_search.py`

Tavily는 캐싱 허용. 5분 LRU.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_tavily_search.py`:
```python
import pytest
from pytest_httpx import HTTPXMock

from shared.http_client import HttpClient
from shared.tavily_search import TavilySearch


@pytest.mark.asyncio
async def test_tavily_search(httpx_mock: HTTPXMock, monkeypatch):
    monkeypatch.setenv("NAVER_CLIENT_ID", "id")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "s")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-test")
    from shared.config import get_settings
    get_settings.cache_clear()

    httpx_mock.add_response(
        json={
            "results": [
                {"title": "Result 1", "url": "https://x.com/1", "content": "snippet"},
            ],
        },
    )

    client = HttpClient()
    tavily = TavilySearch(http=client)
    results = await tavily.search("수면 안마기 후기", max_results=5)

    assert len(results) == 1
    assert results[0]["url"] == "https://x.com/1"
    await client.aclose()


@pytest.mark.asyncio
async def test_tavily_caches_for_5min(httpx_mock: HTTPXMock, monkeypatch):
    """Tavily는 캐싱 OK — 같은 query 두 번째 호출은 캐시."""
    monkeypatch.setenv("NAVER_CLIENT_ID", "id")
    monkeypatch.setenv("NAVER_CLIENT_SECRET", "s")
    monkeypatch.setenv("TAVILY_API_KEY", "tvly-test")
    from shared.config import get_settings
    get_settings.cache_clear()

    httpx_mock.add_response(json={"results": []})

    client = HttpClient()
    tavily = TavilySearch(http=client)
    await tavily.search("test")
    await tavily.search("test")

    assert len(httpx_mock.get_requests()) == 1  # 캐싱됨
    await client.aclose()
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_tavily_search.py -v
```

- [ ] **Step 3: 구현**

`shared/tavily_search.py`:
```python
"""Tavily 검색 클라이언트 — 글로벌 보강 + 한국 부스팅. 5분 캐싱 OK."""
from __future__ import annotations

from typing import Any

from shared.config import get_settings
from shared.http_client import HttpClient
from shared.logging import setup_logger

logger = setup_logger("tavily_search")


class TavilySearch:
    URL = "https://api.tavily.com/search"
    CACHE_TTL = 300  # 5분

    def __init__(self, http: HttpClient) -> None:
        self._http = http
        self._settings = get_settings()

    async def search(
        self,
        query: str,
        *,
        max_results: int | None = None,
        country: str = "south korea",
    ) -> list[dict[str, Any]]:
        max_n = max_results or self._settings.tavily_max_results

        params = {
            "api_key": self._settings.tavily_api_key,
            "query": query,
            "max_results": max_n,
            "country": country,
            "search_depth": "basic",
        }
        # cache_ttl=300 — Tavily 약관 별도, 일반 검색 API 캐싱 허용
        data = await self._http.get(self.URL, params=params, cache_ttl=self.CACHE_TTL)
        results = data.get("results", [])
        logger.info("tavily_search", extra={"query": query, "count": len(results)})
        return results
```

- [ ] **Step 4: 테스트 통과**

```bash
pytest tests/test_tavily_search.py -v
```

- [ ] **Step 5: 커밋**

```bash
git add shared/tavily_search.py tests/test_tavily_search.py
git commit -m "feat(shared): tavily_search.py — Tavily 검색 + 5분 캐싱 (Task 6)"
```

---

## Task 7: `shared/ad_filter.py` — F1 + F4 광고 필터링

**Files:**
- Create: `shared/ad_filter.py`
- Create: `tests/test_ad_filter.py`
- Modify: `servers/gift-curator/data/` 디렉토리 생성 + `ad_keywords.json` 복사

- [ ] **Step 1: 광고 키워드 데이터 복사**

⚠️ Python 모듈명 제약: 하이픈 사용 불가 → `gift_curator` (underscore) 사용. Docker/PlayMCP 등록명은 무관.

```bash
mkdir -p servers/gift_curator/data
cp docs/data/ad_keywords.json servers/gift_curator/data/ad_keywords.json
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_ad_filter.py`:
```python
from shared.ad_filter import AdFilter


def test_detects_explicit_ad_keyword():
    f = AdFilter()
    assert f.is_ad("이거 #광고 표시 있어요") is True
    assert f.is_ad("유료광고 포함 후기") is True


def test_detects_experience_team():
    f = AdFilter()
    assert f.is_ad("체험단으로 받은 제품 후기") is True
    assert f.is_ad("원고료 받고 작성") is True


def test_detects_sponsorship_disclosure():
    f = AdFilter()
    assert f.is_ad("업체로부터 제공받아 작성") is True


def test_clean_text_is_not_ad():
    f = AdFilter()
    assert f.is_ad("정말 좋은 제품이네요. 무릎이 편해졌어요.") is False


def test_filter_keeps_clean_items():
    f = AdFilter()
    items = [
        {"title": "후기 1", "description": "정말 좋아요"},
        {"title": "후기 2", "description": "체험단으로 받았어요"},  # 광고
        {"title": "후기 3", "description": "재구매 의사 있음"},
    ]
    clean = f.filter_items(items)
    assert len(clean) == 2
    titles = [c["title"] for c in clean]
    assert "후기 1" in titles
    assert "후기 3" in titles


def test_multi_source_score():
    """F4 — 같은 키워드를 여러 출처에서 칭찬 → 신뢰도 ↑"""
    f = AdFilter()
    items = [
        {"title": "수면 안마기 좋아요", "link": "https://blog.a.com/1", "description": "추천"},
        {"title": "수면 안마기 만족", "link": "https://blog.b.com/2", "description": "최고"},
        {"title": "수면 안마기 별로", "link": "https://blog.a.com/3", "description": "음"},
        {"title": "수면 안마기 굳", "link": "https://cafe.c.com/4", "description": "사용 후기"},
    ]
    scored = f.aggregate_by_source(items, keyword="수면 안마기")
    # 3개 도메인에서 언급 (a.com 2번, b.com 1번, c.com 1번)
    assert scored["unique_sources"] == 3
    assert scored["total_mentions"] == 4
    assert scored["trust_score"] >= 2  # 단일 출처보다 높음
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pytest tests/test_ad_filter.py -v
```

- [ ] **Step 4: 구현**

`shared/ad_filter.py`:
```python
"""광고/협찬 필터링 — F1 (명시 키워드) + F4 (다중 출처 교차 검증).

⚠️ False negative > False positive — 의심스러우면 제거.
LLM 분류는 호출 에이전트에 위임. 우리는 룰 기반만.
"""
from __future__ import annotations

import json
from pathlib import Path
from typing import Any
from urllib.parse import urlparse

from shared.logging import setup_logger

logger = setup_logger("ad_filter")

_DEFAULT_KEYWORDS_PATH = Path(__file__).resolve().parent.parent / "docs" / "data" / "ad_keywords.json"


class AdFilter:
    """F1 키워드 매칭 + F4 다중 출처 검증."""

    def __init__(self, keywords_path: Path | None = None) -> None:
        path = keywords_path or _DEFAULT_KEYWORDS_PATH
        with open(path, encoding="utf-8") as fp:
            data = json.load(fp)

        self._keywords: list[str] = []
        for category in ("explicit_ad", "experience_team", "group_buy", "sponsorship_disclosure"):
            for entry in data.get(category, []):
                # confidence "강" 또는 "중" 만 사용 (약은 false positive 위험)
                if entry["confidence"] in ("강", "중"):
                    self._keywords.append(entry["keyword"])

    def is_ad(self, text: str) -> bool:
        """텍스트에 광고 키워드 매칭이 있는지."""
        if not text:
            return False
        # 대소문자 무시
        lowered = text.lower()
        for kw in self._keywords:
            if kw.lower() in lowered:
                return True
        return False

    def filter_items(self, items: list[dict[str, Any]]) -> list[dict[str, Any]]:
        """검색 결과 리스트에서 광고 항목 제거."""
        clean = []
        for item in items:
            combined = " ".join(str(item.get(f, "")) for f in ("title", "description", "content", "snippet"))
            if not self.is_ad(combined):
                clean.append(item)
        logger.info("ad_filter_applied", extra={"input": len(items), "output": len(clean)})
        return clean

    def aggregate_by_source(self, items: list[dict[str, Any]], keyword: str) -> dict[str, Any]:
        """F4 — 같은 키워드가 여러 출처(도메인)에 등장 → 신뢰도 점수."""
        domains: set[str] = set()
        mentions = 0
        for item in items:
            link = item.get("link") or item.get("url") or ""
            combined = " ".join(str(item.get(f, "")) for f in ("title", "description", "content"))
            if keyword in combined or keyword.lower() in combined.lower():
                mentions += 1
                if link:
                    domain = urlparse(link).netloc
                    if domain:
                        domains.add(domain)

        return {
            "unique_sources": len(domains),
            "total_mentions": mentions,
            "trust_score": len(domains),  # 도메인 수 = 신뢰도 (단순)
            "domains": sorted(domains),
        }
```

- [ ] **Step 5: 테스트 통과**

```bash
pytest tests/test_ad_filter.py -v
```

Expected: 6 passed.

- [ ] **Step 6: 커밋**

```bash
git add shared/ad_filter.py tests/test_ad_filter.py servers/gift_curator/data/ad_keywords.json
git commit -m "feat(shared): ad_filter.py — F1+F4 광고 필터링 (Task 7)"
```

---

## Task 8: `shared/response_builder.py` — 응답 크기 + 마크다운 빌드

**Files:**
- Create: `shared/response_builder.py`
- Create: `tests/test_response_builder.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_response_builder.py`:
```python
import pytest
from shared.response_builder import ResponseBuilder, ResponseTooLargeError


def test_build_simple_markdown():
    rb = ResponseBuilder()
    result = rb.build([
        ("# 제목", 1),
        ("본문 내용", 2),
    ])
    assert "# 제목" in result
    assert "본문 내용" in result


def test_respects_max_chars():
    rb = ResponseBuilder(max_chars=100)
    sections = [(f"section {i}: " + ("x" * 50), 1) for i in range(10)]
    result = rb.build(sections)
    assert len(result) <= 100 + 50  # 약간 여유 (truncation marker 포함)


def test_truncates_with_marker():
    rb = ResponseBuilder(max_chars=200)
    sections = [(f"section {i}: " + ("x" * 100), 1) for i in range(10)]
    result = rb.build(sections)
    assert "더 보기" in result or "..." in result


def test_priority_keeps_important_sections():
    """priority 낮은 게 먼저 잘림."""
    rb = ResponseBuilder(max_chars=150)
    sections = [
        ("핵심 정보", 1),  # priority 1 (가장 중요)
        ("덜 중요한 정보" + ("y" * 100), 3),  # priority 3
        ("중간 정보" + ("z" * 50), 2),
    ]
    result = rb.build(sections)
    assert "핵심 정보" in result
    # priority 3은 짤렸을 수 있음
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_response_builder.py -v
```

- [ ] **Step 3: 구현**

`shared/response_builder.py`:
```python
"""Tool 응답 마크다운 빌드 + 24k 한계 검사.

PlayMCP 규칙: Response 24,000자 초과 시 에러 → 반려.
우리는 22,000자 안전 한계 (2k 버퍼).
"""
from __future__ import annotations

from typing import Iterable

from shared.config import get_settings
from shared.logging import setup_logger

logger = setup_logger("response_builder")


class ResponseTooLargeError(Exception):
    pass


class ResponseBuilder:
    """priority가 낮은(숫자 작은) 섹션이 우선 보존. 큰 priority는 truncate 가능."""

    TRUNCATION_MARKER = "\n\n_...(이하 생략. 더 자세한 정보가 필요하면 다른 Tool 호출)_"

    def __init__(self, max_chars: int | None = None) -> None:
        self._max = max_chars or get_settings().response_max_chars

    def build(self, sections: Iterable[tuple[str, int]]) -> str:
        """sections: [(content, priority), ...]. priority 작을수록 중요."""
        # priority 순 정렬 (작은 게 먼저)
        sorted_secs = sorted(sections, key=lambda s: s[1])

        out: list[str] = []
        current_len = 0
        truncated = False

        for content, _priority in sorted_secs:
            piece = content + "\n\n"
            if current_len + len(piece) > self._max:
                truncated = True
                break
            out.append(piece)
            current_len += len(piece)

        result = "".join(out).rstrip()
        if truncated:
            result += self.TRUNCATION_MARKER

        if len(result) > self._max + len(self.TRUNCATION_MARKER):
            logger.warning("response_too_large", extra={"length": len(result)})
            raise ResponseTooLargeError(f"Response {len(result)} > {self._max}")

        return result
```

- [ ] **Step 4: 테스트 통과**

```bash
pytest tests/test_response_builder.py -v
```

- [ ] **Step 5: 커밋**

```bash
git add shared/response_builder.py tests/test_response_builder.py
git commit -m "feat(shared): response_builder.py — 24k 한계 + 우선순위 truncate (Task 8)"
```

---

## Task 9: 효도비서 서버 골격 (`servers/hyodo/server.py`)

**Files:**
- Create: `servers/hyodo/__init__.py` (빈 파일)
- Create: `servers/hyodo/server.py`
- Create: `tests/test_hyodo_server.py`

이 Task는 Tool 없는 빈 서버. 실행만 되면 OK. 실제 Tool은 Task 13~17에서 추가.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_hyodo_server.py`:
```python
from servers.hyodo.server import mcp


def test_mcp_instance_exists():
    assert mcp is not None
    assert mcp.name == "Hyodo Secretary(효도비서)"


def test_server_has_no_tools_yet():
    # Phase 2.1에선 골격만, Tool 없음
    tools = list(mcp._tools) if hasattr(mcp, "_tools") else []
    # 정확한 attr 이름은 FastMCP 버전마다 다를 수 있음 — 그냥 mcp 객체 생성만 검증
    assert mcp is not None
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_hyodo_server.py -v
```

- [ ] **Step 3: 빈 init + 구현**

```bash
mkdir -p servers/hyodo
touch servers/hyodo/__init__.py
```

`servers/hyodo/server.py`:
```python
"""Hyodo Secretary(효도비서) MCP Server.

⚠️ Phase 2.1: 골격만. Tool은 Phase 2.2에서 추가.
"""
from __future__ import annotations

from mcp.server.fastmcp import FastMCP

from shared.logging import setup_logger

logger = setup_logger("hyodo_server")

mcp = FastMCP(
    "Hyodo Secretary(효도비서)",
    instructions=(
        "효도비서: 부모님과 멀리 사는 자녀를 위한 카카오톡 도우미. "
        "안부 메시지 생성과 의심 메시지 사기 판단이 두 핵심 기능. "
        "모든 Tool은 빠른 데이터 조회/포맷팅만 — 자연어 추론은 호출 에이전트가 담당."
    ),
)


# Tool 등록은 Phase 2.2에서 추가됨
# from servers.hyodo.tools import compose_anbu, check_suspicious_message, ...


def main() -> None:
    """서버 진입점 — Streamable HTTP 모드로 실행."""
    logger.info("hyodo_server_starting")
    mcp.run(transport="streamable-http")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트 통과**

```bash
pytest tests/test_hyodo_server.py -v
```

- [ ] **Step 5: 로컬 실행 검증 (수동)**

```bash
python -m servers.hyodo.server
```

Expected: 서버가 시작되고 포트에 listen 상태. `Ctrl+C`로 종료.

- [ ] **Step 6: 커밋**

```bash
git add servers/hyodo/__init__.py servers/hyodo/server.py tests/test_hyodo_server.py
git commit -m "feat(hyodo): MCP 서버 골격 생성 (Task 9)"
```

---

## Task 10: 선물고민러 서버 골격 (`servers/gift-curator/server.py`)

**Files:**
- Create: `servers/gift_curator/__init__.py` (Python module 이름엔 하이픈 X → `gift_curator` underscore)
- Create: `servers/gift_curator/server.py`
- Create: `tests/test_gift_curator_server.py`

⚠️ Python 모듈명은 underscore만 가능. Docker / PlayMCP 등록 시 디렉토리는 `gift-curator` (하이픈)이지만 Python import는 `gift_curator`.

→ 해결: 디렉토리는 `gift_curator`로, Dockerfile에서 `WORKDIR /app && CMD python -m servers.gift_curator.server`.
(PlayMCP에서 보이는 이름은 어차피 MCP 서버명 `Gift Curator(선물고민러)`이라 디렉토리명과 무관)

- [ ] **Step 1: 디렉토리 + __init__.py**

(Task 7에서 이미 `servers/gift_curator/data/` 만들었음. 여기선 __init__.py만 추가)

```bash
touch servers/gift_curator/__init__.py
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_gift_curator_server.py`:
```python
from servers.gift_curator.server import mcp


def test_mcp_instance_exists():
    assert mcp is not None
    assert mcp.name == "Gift Curator(선물고민러)"
```

- [ ] **Step 3: 구현**

`servers/gift_curator/server.py`:
```python
"""Gift Curator(선물고민러) MCP Server.

⚠️ Phase 2.1: 골격만. Tool은 Phase 2.2에서 추가.
"""
from __future__ import annotations

from mcp.server.fastmcp import FastMCP

from shared.logging import setup_logger

logger = setup_logger("gift_curator_server")

mcp = FastMCP(
    "Gift Curator(선물고민러)",
    instructions=(
        "선물고민러: 광고/협찬 없는 진짜 칭찬받은 선물을 큐레이션하는 도우미. "
        "관계·예산·상황을 바탕으로 카카오 선물하기 MCP의 SearchGift와 자연 연계되는 "
        "키워드·가격대·추천 이유를 반환. 모든 Tool은 데이터 조회/필터링만 담당."
    ),
)


# Tool 등록은 Phase 2.2에서 추가됨


def main() -> None:
    logger.info("gift_curator_server_starting")
    mcp.run(transport="streamable-http")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트 통과**

```bash
pytest tests/test_gift_curator_server.py -v
```

- [ ] **Step 5: 로컬 실행 검증**

```bash
python -m servers.gift_curator.server
```

- [ ] **Step 6: 커밋**

```bash
git add servers/gift_curator/__init__.py servers/gift_curator/server.py servers/gift_curator/data/ tests/test_gift_curator_server.py
git commit -m "feat(gift-curator): MCP 서버 골격 생성 (Task 10)"
```

---

## Task 11: Phase 2.1 통합 검증 + 회고

- [ ] **Step 1: 전체 테스트 실행**

```bash
pytest -v
```

Expected: 모든 테스트 통과 (Task 2~10에서 작성한 모든 테스트).

- [ ] **Step 2: 두 서버 동시 실행 가능 검증**

별도 터미널 2개에서:
- Terminal 1: `python -m servers.hyodo.server`
- Terminal 2: `python -m servers.gift_curator.server`

각각 다른 포트에 listen하는지 확인 (FastMCP 기본 8000번 충돌 가능 → MCP 환경 변수로 포트 분리 필요). 충돌 시 `MCP_HOST=0.0.0.0 MCP_PORT=8001` 같은 환경 변수로 분리.

- [ ] **Step 3: 디렉토리 구조 최종 확인**

```bash
tree -L 3 -I '.venv|__pycache__|*.egg-info|.git'
```

Expected:
```
agentic-player-10/
├── .env (gitignore됨)
├── .env.example
├── .gitignore
├── CLAUDE.md
├── README.md
├── pyproject.toml
├── docs/
│   ├── data/
│   ├── design/
│   ├── plans/
│   └── specs/
├── shared/
│   ├── __init__.py
│   ├── ad_filter.py
│   ├── config.py
│   ├── http_client.py
│   ├── logging.py
│   ├── naver_search.py
│   ├── response_builder.py
│   └── tavily_search.py
├── servers/
│   ├── hyodo/
│   │   ├── __init__.py
│   │   └── server.py
│   └── gift_curator/
│       ├── __init__.py
│       ├── data/
│       │   └── ad_keywords.json
│       └── server.py
└── tests/
    ├── __init__.py
    ├── test_ad_filter.py
    ├── test_config.py
    ├── test_gift_curator_server.py
    ├── test_http_client.py
    ├── test_hyodo_server.py
    ├── test_logging.py
    ├── test_naver_search.py
    ├── test_response_builder.py
    └── test_tavily_search.py
```

- [ ] **Step 4: Phase 2.1 회고 + 다음 phase 준비**

회고 체크리스트 (CLAUDE.md 지침 #5 — 계속 개선):
- [ ] 어떤 컴포넌트가 가장 시간 많이 걸렸나? (다음에 더 깊이 설계할 영역)
- [ ] 테스트가 빠진 시나리오 있나? (Phase 2.2 시작 전 보강 권장)
- [ ] CLAUDE.md 갱신할 발견사항 있나? (예: FastMCP 특정 버전 이슈)
- [ ] Phase 2.2 시작 전 Claude/GPT에 외주할 작업 정리

- [ ] **Step 5: Phase 2.1 종료 커밋**

```bash
git commit --allow-empty -m "chore: Phase 2.1 공유 인프라 완료 — Phase 2.2 진입 준비"
```

---

## Phase 2.1 완료 후 다음 단계

Phase 2.2 (Tools 구현) 계획 문서는 **Phase 2.1 완료 후 작성**합니다. 이유:
- Phase 2.1에서 발견되는 인터페이스 변경/제약을 반영해야 함
- shared/ 모듈 시그니처가 확정된 후 Tool 명세 작성이 정확해짐
- iterative-engineering 원칙 (수술적 변경 + 단계별 검증)

Phase 2.1 완료 시점에 사용자에게 알리고, Phase 2.2 plan 작성 진입.

---

## 미해결 이슈 (Phase 2.1 시작 시 결정)

| # | 이슈 | 결정 방법 |
|---|---|---|
| 2.1-U1 | FastMCP의 streamable-http transport 포트 기본값 + 충돌 시 환경 변수 | Task 11에서 실측 |
| 2.1-U2 | `lru_cache` + monkeypatch 충돌 시 fixture 패턴 | Task 2에서 실측 |
| 2.1-U3 | 디렉토리명 `gift_curator` (Python) vs `gift-curator` (Docker/PlayMCP) 일관성 | Task 10에서 underscore 채택 |
